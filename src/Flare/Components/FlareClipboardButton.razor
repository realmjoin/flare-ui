@namespace Flare

<button @attributes="InputAttributes" @onclick="HandleClick">
    @if (ChildContent is not null)
    {
        @ChildContent
    }
    else
    {
        <span>Copy to clipboard</span>
    }
</button>
