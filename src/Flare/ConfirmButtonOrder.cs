namespace Flare;

/// <summary>
/// Controls the visual order of buttons in a confirm dialog.
/// </summary>
public enum ConfirmButtonOrder
{
    /// <summary>Cancel on left, confirm/primary on right (default).</summary>
    ConfirmRight,

    /// <summary>Confirm/primary on left, cancel on right.</summary>
    ConfirmLeft,
}
